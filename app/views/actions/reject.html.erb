<h2>Please provide reasons for rejection.</h2>

<div class="form-wrapper">
    
    <%= form_for(:action, url: action_path(@action.id), method: :patch) do |f| %>
        <%= f.text_area :closeout, class: 'form-control' %>
        <br/>
        <%= f.submit "Return Action", class: "btn btn-primary" %>
    <% end %>
    
</div>

    