<% provide(:button_text, "Update Event") %>
<% provide(:path, event_path) %>
<% provide(:show_flag, "" ) %>
<% provide(:method_text, :patch) %>

<h1>UNOR Information</h1>

<div class="form-wrapper">

    <%= render 'layouts/form_event' %>

    <br/>
  
<% if current_user.team != "SHEQ" %>
    <table>
        <tr>
          <td>
            <%= link_to "Request UNOR Close-out", closeplease_path(@events.id), method: :patch, class:"btn btn-primary" %>
          </td>
        </tr>
    </table>
<% elsif current_user.team == "SHEQ" %>
     <table>
        <tr>
          <td>
            <%= link_to "Request UNOR Close-out", closeplease_path(@events.id), method: :patch, class:"btn btn-primary" %><br/><br/>
            <% if @events.close_request_flag %>
                <%= link_to "Reject Close Request", reject_path(@events.id), class:"btn btn-primary" %>
                <% if @events_action_check == "complete" %>
                	<br/><br/>
                	<%= link_to "Close UNOR", close_path(@events.id), method: :patch, class:"btn btn-primary" %>
            	<% end %>
            <% end %>
          </td>
        </tr>
    </table>
<% end %>

</div>